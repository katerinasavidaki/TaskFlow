package com.taskflow.backend.core.enums;

/**
 * Enum representing the available user roles in the system.
 * <p>
 * Each role defines the level of access and permissions
 * a user has within the application.
 */
public enum RoleType {
    ADMIN,
    MANAGER,
    TEAM_LEADER,
    MEMBER
}
